# Core dependencies: everything needed to run the app in fully-local, key-free
# mode (deterministic NL-to-SQL, in-memory vector search, guardrails, Plotly,
# Streamlit UI). This is what CI and Streamlit Community Cloud install - it is
# light and pulls NO torch, so the free deploy builds fast and reliably.
#
# For the full cloud path (OpenAI + Qdrant + real embeddings) install
# requirements-cloud.txt instead.
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
duckdb>=0.10
sqlglot>=23.0
plotly>=5.20
streamlit>=1.33
python-dotenv>=1.0
PyYAML>=6.0
